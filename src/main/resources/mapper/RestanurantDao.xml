<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.zee.ordering.dao.RestanurantDao">

	<select id="getAllResturantDepartmentIds" resultType="String">
		select GROUP_CONCAT(department_ids) from t_restaurant_config
	</select>
	
	
	
</mapper>